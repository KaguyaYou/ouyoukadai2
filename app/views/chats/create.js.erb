<!--/*.messageを部分更新。appendメソッドで要素を追加 */-->
$('.message').append("<th class="card-body bg-warning"><p style='text-align: left;'><%= @chat.message %></p></th>");

<!--/*チャット投稿後、フォームの値に前回の投稿内容が残らないようにする */-->
$('input[type=text]').val("")